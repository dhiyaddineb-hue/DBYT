# ---- DBYT: Professional Video Dubbing Pipeline ----
# Core web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4

# YouTube download & metadata
yt-dlp[default]==2026.8.19
bgutil-ytdlp-pot-provider==1.3.1
# Browser-based PO-token provider; useful when GitHub Actions IPs are bot-challenged.
yt-dlp-getpot-wpc==1.0.0

# Speech-to-text (Whisper) — CPU friendly via CTranslate2
faster-whisper==1.0.3

# Translation
deep-translator==1.11.4
langcodes==3.4.0

# ---- OFFLINE translation (solves "Google blocked" — uncomment to enable) ----
# argostranslate        # Argos Translate — offline neural MT (en↔ar)
# ctranslate2           # NLLB-200 (best Arabic quality, offline)
# transformers
# sentencepiece

# Text-to-speech engines
edge-tts==6.1.12
# ElevenLabs (optional, premium emotional voice) — uncomment to enable
# elevenlabs==1.6.1
# Coqui XTTS voice cloning (optional, GPU recommended) — uncomment to enable
# TTS==0.22.0
# Suno Bark emotional TTS (optional, heavy) — uncomment to enable
# bark @ git+https://github.com/suno-ai/bark.git

# ---- OFFLINE TTS (solves "TTS host blocked" — uncomment to enable) ---------
# piper-tts              # Piper (Rhasspy) — fast offline neural voices incl. Arabic

# Audio/Video processing
# (ffmpeg is a system dependency, install via apt: sudo apt install ffmpeg)
pydub==0.25.1
